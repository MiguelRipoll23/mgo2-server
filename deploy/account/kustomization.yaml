apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

# The deploy pipeline rewrites `newTag` with the commit SHA it published. See
# deploy/README.md for the seed value and why it must not survive a sync.
images:
  - name: ghcr.io/miguelripoll23/mgo2-server/mgo2-account-lobby-server
    newTag: 653c85da7749dc1535ca102698c143fe82c0fb35
