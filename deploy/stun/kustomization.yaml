apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

# The deploy pipeline rewrites `newTag` with the commit SHA it published. See
# deploy/README.md for the seed value and why it must not survive a sync.
images:
  - name: ghcr.io/miguelripoll23/mgo2-server/mgo2-stun
    newTag: 61e73bf2202e7a564611145316d6605abdd58cfa
