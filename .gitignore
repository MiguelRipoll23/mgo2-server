appsettings.json
# The dotenv of a deployment directory the install scripts create, which holds
# the JWT secret. It is written next to compose.yaml, outside any clone.
deployment.env
bin/
obj/

# Output of scripts/run-linux-macos.sh and scripts/run-windows.ps1.
.logs/

# Python bytecode left behind by the analysis scripts in tools/.
__pycache__/
