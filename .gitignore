appsettings.json
# The settings of a deployment directory the install scripts create, which
# hold the JWT secret. They are written next to compose.yaml, outside any clone.
deployment.json
bin/
obj/

# Output of scripts/run-linux-macos.sh and scripts/run-windows.ps1.
.logs/

# Python bytecode left behind by the analysis scripts in tools/.
__pycache__/
