// Registers every UDP peer command handler (imported for side effects, like
// the TCP servers' ./commands imports).

import "./accept-handshake.ts";
import "./ack-keep-alive.ts";
import "./handle-player-profile.ts";
